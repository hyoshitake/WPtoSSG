# Implementation Plan

README.md と AGENTS.md をもとに、レビューしやすい単位で issue 化できる粒度に分割した実装計画です。

## Issue 1: 要件と責務境界の整理
- [x] README.md と AGENTS.md の内容を基準に、対象範囲・非対象範囲・完了条件を確定する
- [x] web / worker / shared / config / infra / docs の責務を明文化する

## Issue 2: 共通型と設定の土台を作る
- [x] Graph、Job、Stage、Diagnostic の共有型を定義する
- [x] ドメイン判定、CDNマッピング、静的化ルールの設定置き場を決める

## Issue 3: ジョブ状態管理とイベント記録を実装する
- [ ] `convert-site` のステージ定義を固定する
- [ ] `job_events` に進捗・警告・失敗理由を残す仕組みを作る
- [ ] SSE 配信用のイベント形式を合わせる

## Issue 4: クロール用Graph生成を実装する
- [ ] URL列挙ではなくノード/エッジ構造で保存する
- [ ] 同一ドメインのみ追跡し、外部リンクは保持する
- [ ] 多言語パスやクエリURLの扱いを整理する

## Issue 5: Playwright レンダリングとスナップショットを実装する
- [ ] JS実行後DOMの取得を安定化する
- [ ] 無限スクロールと lazy load 対応を組み込む
- [ ] ページ単位失敗を継続可能な形で記録する

## Issue 6: 内部アセット収集と書き換えを実装する
- [ ] 同一ドメインのアセット取得を行う
- [ ] 外部参照は原則維持する
- [ ] 既知ライブラリの CDN 変換ルールを適用する

## Issue 7: 静的化困難性の診断を実装する
- [ ] fetch/XHR、form、login兆候を収集する
- [ ] `risk_level` / `reasons` / `evidence` を出力する
- [ ] 断定ではなく根拠付きの診断にする

## Issue 8: Google Drive の current/archive 運用を実装する
- [ ] `current` 配下への配置を実装する
- [ ] 再実行時に `archive` 退避してから新しい `current` を作る
- [ ] ローテーション失敗時の再試行を考慮する

## Issue 9: `FINALIZE` とレポート生成を実装する
- [ ] `report.json` を必ず出力する
- [ ] 成功/失敗内訳を残す
- [ ] ジョブ完了状態を確実に更新する

## Issue 10: Web UI と SSE 連携を実装する
- [ ] ジョブ作成、進捗表示、診断結果表示を実装する
- [ ] `GET /api/jobs/:id/events` で SSE を配信する
- [ ] 再接続とハートビートを扱う

## Issue 11: 受入条件と検証手順を整備する
- [ ] 100ページ未満の単一サイトで完走できることを確認する
- [ ] 2回目実行で archive ローテーションが成功することを確認する
- [ ] SSE で進捗受信できることを確認する
- [ ] 診断結果が UI に表示されることを確認する
